apiVersion: v1
kind: Service
metadata:
  name: rdicidr-headless
  namespace: production
spec:
  clusterIP: None
  selector:
    app: rdicidr
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
